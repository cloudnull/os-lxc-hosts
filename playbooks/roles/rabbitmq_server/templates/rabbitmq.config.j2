[
  {rabbit, [
    {loopback_users, []},
    {cluster_nodes, {
      [ {% for host in groups[rabbitmq_main_group_name] %}'rabbit@{{ hostvars[host]['container_address'] }}'{% if not loop.last %}, {% endif %}{% endfor %}], disc}
    }
  ]}
].
